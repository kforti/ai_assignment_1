In the heavy_n_queens directory, run:

    python heavy_n_queens.py [input_path] [algorithm (hill_climb or astar)] [heuristic (h1 or h2 or h3)]

Replace the brackets with your value, brackets should not be in your command.

The output of running the above command will be printed in the console and persisted to disk on a file in the current working directory with the name of the algorithm, heuristic and time/ date the command was run.
